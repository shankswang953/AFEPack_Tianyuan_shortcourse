# Shared example resources

This directory contains checked-in resources shared by multiple examples.
It has no executable scripts and produces no output.

`mesh/` contains reusable EasyMesh input data. Do not place generated solver
results here; each numbered example writes to its own `output/` directory.
