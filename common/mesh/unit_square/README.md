# Unit-square root mesh

`D.n`, `D.e`, and `D.s` are the checked-in EasyMesh node, element, and
side files for the unit square. Use the basename without a suffix:

```bash
./run.sh ../common/mesh/unit_square/D
```

These files are inputs and should not be overwritten by example outputs.
